---
title: "10. OpenManus"
original: "10. OpenManus"
translated: true
---

# 10. OpenManus

[Github 地址](#)
免费 API 方法？从 Hyperbolic 或 Groq 设置 API 密钥 = 免费！

### 设置说明

**第 1 步：**
```
conda create -n open_manus python=3.12
conda activate open_manus
```

**第 2 步：**
```
git clone https://github.com/mannaandpoem/OpenManus.git
cd OpenManus
```

**第 3 步：**
```
pip install -r requirements.txt
```

**第 4 步：**
```
cp config/config.example.toml config/config.toml
```

**第 5 步：**
```
nano config/config.toml
```

**第 6 步：** 设置下面的全局 LLM 配置 > 插入你的 API 密钥

**第 7 步：** `Control + X` > `Y` > `Enter`

**第 8 步：**
```
python main.py
```

**第 9 步：** 测试

**第 10 步：** 有问题？截图问题 + 粘贴到终端，附上 GitHub 说明。

### 如何设置 API 密钥：

```toml
# Global LLM configuration
[llm]
model = "gpt-4o"
base_url = "https://api.openai.com/v1"
api_key = "sk-..." # Replace with your actual API key
max_tokens = 4096
temperature = 0.0

# Optional configuration for specific LLM models
[llm.vision]
model = "gpt-4o"
base_url = "https://api.openai.com/v1"
api_key = "sk-..." # Replace with your actual API key
```

### 示例提示词

```
Build an SEO Cost Calculator in HTML and preview it in Chrome
```
