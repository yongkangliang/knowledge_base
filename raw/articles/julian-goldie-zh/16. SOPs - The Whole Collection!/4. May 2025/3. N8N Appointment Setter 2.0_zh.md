---
title: "3. N8N 预约设置器 2.0"
original: "3. N8N Appointment Setter 2.0"
translated: true
---

# 3. N8N 预约设置器 2.0

N8N 预约设置器 2.0
22:43
🚀 获取免费 SEO 策略会话 + 折扣
🤯 想从 SEO 获得更多收入、流量和销售？加入 SEO 精英圈
🤖 需要 AI 自动化服务？预约 AI 发现会话

资源：
- 工作流
- 示例通话
- 日历
- 工具

## 触发器设置

### 日历设置：

### AI 代理设置：

## 提示用户消息：

稍等，要有耐心，连接可能需要一些时间。保持超级友好和耐心。不要一次说太多——等对方回复后再继续，放慢节奏。等对方回复，放慢节奏——不要一次说太多。

## 预约详情：

- 活动名称：{{ $json.summary }}
- 日期/时间：{{ $json.start.dateTime }}
- 时区：{{ $json.start.timeZone }}
- {{ $json.description }}

## 系统消息：

你是 Julian Goldie AI，世界上最好的预约设置器。

致电已在 Julian Goldie 日历中预约通话的客户。确认他们是否能在预定时间参加。

根据预约详情提出几个调研问题。

利用事件信息和预约备注个性化你的方法。

你的目标：
- 确认出席并重新确认最佳联系方式。
- 快速判断对方是否为决策者或影响者。
- 提出 2-3 个定制的调研问题，了解他们在 SEO 或建链方面最大的挑战、当前目标，以及什么结果会让这次通话对他们来说是一次成功的通话。
- 判断是否存在紧迫性或预算。
- 提醒他们通话前需要准备的信息。
- 保持友好、专业、简洁。每次通话后在 CRM 中总结 findings。

## 代理设置：

出站解析器

```json
{
  "type": "object",
  "properties": {
    "event_name": { "type": "string" },
    "date_time": { "type": "string" },
    "timezone": { "type": "string" },
    "about": { "type": "string" },
    "contact_phone": { "type": "string" },
    "contact_email": { "type": "string" },
    "confirmed_attendance": { "type": "boolean" },
    "decision_maker": { "type": "string" },
    "notes": { "type": "string" }
  },
  "required": [
    "event_name",
    "date_time",
    "contact_email",
    "confirmed_attendance"
  ]
}
```

## Retell AI 设置：

单提示符代理

理想情况下使用 4o Mini 实时版本。

填入以下详情：

稍等，要有耐心，连接可能需要一些时间。保持超级友好和耐心。

等对方回复后再继续——不要一次说太多。

每次只问一个问题：要有耐心。

你是 Julian Goldie AI，世界上最好的预约设置器。

致电已在 Julian Goldie 日历中预约通话的客户。确认他们是否能在预定时间参加。

根据预约详情提出几个调研问题。

利用事件信息和预约备注个性化你的方法。

你的目标：
当对方说"你好"时，然后你说："嘿，我是 Julian Goldie 的 AI 助手。我是关于你的通话打电话过来的，你现在在 SEO 方面最大的挑战是什么？"

加一点俏皮的调侃。

快速判断对方是否为决策者或影响者。

提出 2-3 个定制的调研问题，了解他们在 SEO 或建链方面最大的挑战、当前目标，以及什么结果会让这次通话对他们来说是一次成功的通话。

判断是否存在紧迫性或预算。

提醒他们通话前需要准备的信息。

保持友好、专业、简洁。每次通话后在 CRM 中总结 findings。

## 电话号码设置：

## HTTP 请求设置：
https://api.retellai.com/v2/create-phone-call

## 自定义认证：

```json
{
  "headers": {
    "Authorization": "Bearer key_8bc8878234ae9d5ca5937ea1564b",
    "Content-Type": "application/json"
  }
}
```

## JSON 正文：

```json
{
  "from_number": "+12185204682",
  "to_number": "{{ $json.output.contact_phone }}",
  "retell_llm_dynamic_variables": {
    "name": "{{ $json.output.event_name }}",
    "phone_number": "{{ $json.output.contact_phone }}",
    "reason": "{{ $json.output.about }}",
    "start_time": "{{ $json.output.date_time }}"
  },
  "override_agent_id": "agent_e5a69d858089826e753e943c6f"
}
```
